# Mesh Project Instructions

## Commit Handling
When you ask for a commit to be generated:
1. Generate the commit message describing the changes
2. Append the commit message to `commits.txt`
3. Do NOT call git directly to commit - only generate the message and add to commits.txt
